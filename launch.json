{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "chrome",
        "request": "launch",
        "name": "Launch Chrome against localhost",
        "url": "http://localhost:5500", // Adjust the port number according to your Live Server configuration
        "webRoot": "${workspaceFolder}"
      }
    ]
  }